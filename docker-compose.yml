version: '3.3'

services:

  cli:
    image: nldicrawlerpy:test 
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    environment:
      NLDI_DB_HOST: "172.18.0.1"
      NLDI_DB_PORT: "5432"
      NLDI_DB_USER: "nldi_schema_owner"
      NLDI_DB_PASS: "changeMe"
      NLDI_DB_NAME: "nldi"
    command: 'poetry run nldi-cli -vv sources'

    container_name: nldi-cli
